# Production environment configuration
database:
  host: 127.0.0.1
  port: 3306
  user: bury_user
  password: bury_password_2024
  database: bury

jwt:
  secret: bury_jwt_secret_key_2024_secure

server:
  port: 8080
  # tls_cert_file: "/etc/ssl/certs/bury.crt"
  # tls_key_file: "/etc/ssl/certs/bury.key"

cors:
  allowed_origins:
    - "https://buried.app"
    - "https://www.buried.app"
    - "capacitor://localhost"

security:
  snowflake_node_id: 1
  rate_limit_per_min: 30